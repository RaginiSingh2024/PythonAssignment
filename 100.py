# Import Math Module
import math

num = float(input("Enter a number: "))

sqrt = math.sqrt(num)
floor = math.floor(num)
ceil = math.ceil(num)

print(f"Square root: {sqrt}")
print(f"Floor: {floor}")
print(f"Ceiling: {ceil}")